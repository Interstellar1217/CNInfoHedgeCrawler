# 插件使用指南

本项目已封装为两个平台的插件，可以根据需求选择使用。

---

## 1. Dify 插件

**适用场景**：为 AI Agent 提供工具，让 AI 自动调用爬虫获取公告信息

**平台**：[Dify.ai](https://dify.ai) - AI 应用开发平台

### 目录结构

```
dify_plugin/cninfo_hedge/
├── __init__.py                 # 插件入口
├── main.py                     # 工具实现
├── provider.yaml               # 提供商配置
├── tools.yaml                  # 工具列表
├── tools/
│   └── search_announcements.yaml  # 工具定义和 Schema
├── icon.svg                    # 插件图标
└── requirements.txt            # 依赖
```

### 部署方式

详见 [Dify 插件 README](dify_plugin/README.md)

---

## 2. Astrbot 插件

**适用场景**：在 QQ/微信等 IM 聊天工具中，通过命令查询公告

**平台**：[Astrbot](https://github.com/Soulter/Astrbot) - 聊天机器人框架

### 目录结构

```
astrbot_plugin/
├── __init__.py      # 插件入口
├── main.py          # 插件主逻辑
├── plugin.json      # 插件元数据
└── README.md        # 说明文档
```

### 使用命令

| 命令 | 说明 |
|------|------|
| `/套保查询` | 搜索公告 |
| `/套保` | 快捷查询 |

详见 [Astrbot 插件 README](astrbot_plugin/README.md)

---

## 对比

| 特性 | Dify 插件 | Astrbot 插件 |
|------|----------|-------------|
| 使用方式 | AI Agent 自动调用 | 聊天命令触发 |
| 返回格式 | JSON 结构化数据 | 聊天消息 |
| 适用平台 | Dify AI 平台 | QQ/微信/Telegram 等 |
| 配置复杂度 | 中等 | 简单 |

---

## 依赖说明

两个插件都依赖原项目的 `config.py` 配置文件，包括：

- URL 配置
- 请求头（含 TLS 指纹）
- 延时和重试配置
- 企业微信 Webhook（可选）

---

## 开发说明

### Dify 插件开发

1. 在 `tools/search_announcements.yaml` 中定义输入输出 Schema
2. 在 `main.py` 中实现 `invoke()` 函数
3. 本地运行 `python main.py` 测试

### Astrbot 插件开发

1. 在 `plugin.json` 中配置元数据
2. 在 `main.py` 中继承 `BasePlugin` 并注册命令
3. 使用 `@event_message_type` 装饰器注册处理器

---

## 常见问题

### Q: 插件无法加载？
A: 检查是否安装了依赖：`pip install -r requirements.txt`

### Q: 查询结果为空？
A: 检查日期范围是否正确，或尝试扩大日期范围

### Q: 如何添加新的查询条件？
A: 修改 `config.py` 中的 `get_search_params()` 方法，并在插件的 Schema 中添加对应参数

---

## 贡献

欢迎提交 Issue 和 PR！

## License

MIT
